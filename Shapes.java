package methodHomeWork;

public class Shapes {
    /* Create 1 class in which create a methods that will calculate the area of Method name must
    be called calculateArea Rectangle Square Use separate class to test your code */

double calculateAreaRectangle(double length,double width){
    double area=length*width;
    return area;
}
int calculateAreaSquare(int length,int width){
    int area=length*width;
    return area;
}

}
